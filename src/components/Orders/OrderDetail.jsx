 
  
  export const OrderDetail = ({orderDetail}) => {
    return (
      <div>OrderItem - {orderDetail.id} - {orderDetail.productId} - {orderDetail.quantity} - {orderDetail.id} - {orderDetail.price}</div>
    )
  }
  